apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-todo-pvc
  namespace: project
spec:
  storageClassName: postgres-todo-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi